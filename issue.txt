================================================================================
           LEXVERA HRMS — BUSINESS FLOW vs ACTUAL CODE COMPARISON
                     GAP ANALYSIS & ISSUE TRACKER
                     Date: 18-Sep-2026 | Last QA Run: 22-Sep-2026
                     Last Fixed: 22-Sep-2026 (Patch #1)
================================================================================

================================================================================
PART 1: CURRENT STATE SNAPSHOT
================================================================================

Tech Stack:
  Frontend: React 18 + TypeScript + Vite + TailwindCSS + Axios + Socket.io-client
  Backend:  Node.js + Express + TypeScript + Prisma ORM + SQLite (dev.db)
  Auth:     JWT (accessToken 15min + refreshToken 7-day, auto-refresh implemented)
  Realtime: Socket.io WebSocket
  Hosting:  Local dev only (no Docker/Nginx yet)

Servers (both running):
  Backend:  http://localhost:5000  (tsx watch, hot-reload)
  Frontend: http://localhost:5173  (Vite dev server)

Module Count:
  Frontend Views: 22 | Backend Modules: 22 | API Routes: 22+ endpoints

================================================================================
PART 2: ACTIVE ISSUES (Still Open — Needs Work)
================================================================================

[ISSUE-001] ARCHITECTURE: MongoDB ki jagah SQLite use ho raha hai
CATEGORY: Architecture
SEVERITY: CRITICAL
DETAIL:
  Business-Flow mein MongoDB mention hai. Abhi SQLite via Prisma use ho raha hai.
  SQLite concurrent writes handle nahi karta enterprise scale pe.
  Plan: Migrate to PostgreSQL first (easier than MongoDB with Prisma).

[ISSUE-002] ARCHITECTURE: Redis bilkul nahi hai
CATEGORY: Architecture
SEVERITY: HIGH
DETAIL:
  No caching, no session store, no pub/sub.
  Har request direct DB hit karta hai.
  Real-time event processing Redis ke bina limited hai.

[ISSUE-003] ARCHITECTURE: Rate Limiting implement nahi hai
CATEGORY: Architecture / Security
SEVERITY: MEDIUM
DETAIL:
  express-rate-limit ya koi bhi rate limiter nahi.
  Brute force attacks possible on /auth/login.
  Fix: npm install express-rate-limit and apply on /auth routes.

[ISSUE-004] ARCHITECTURE: Queue System (BullMQ) nahi hai
CATEGORY: Architecture
SEVERITY: MEDIUM
DETAIL:
  jobs/scheduler.ts cron hai, proper BullMQ queue nahi.
  Email send, PDF generate — sab synchronous ya missing hain.

[ISSUE-005] ARCHITECTURE: File Storage implement nahi hai
CATEGORY: Architecture / Feature
SEVERITY: HIGH
DETAIL:
  uploads/ folder exist karta hai lekin koi multer/S3 integration nahi.
  Employee avatar, leave medical documents, task attachments — sab DB links only.
  Fix: multer + local storage for dev, S3/Cloudinary for production.

[ISSUE-006] BACKEND STRUCTURE: Strict Module Architecture follow nahi ho raha
CATEGORY: Backend Structure
SEVERITY: CRITICAL
DETAIL:
  Har module mein SIRF controller.ts hai. No service/repository/schema layers.
  Business logic + DB queries + routes ek hi file mein — fat controller anti-pattern.
  Fix: Gradually refactor each module: controller -> service -> repository.

[ISSUE-011] BACKEND: Staffing Conflict Check missing in Leave Flow
CATEGORY: Backend Business Logic
SEVERITY: MEDIUM
DETAIL:
  Leave apply flow mein koi team staffing conflict check nahi hai.
  Business-Flow clearly define karta hai — kitne log ek date pe absent?
  Fix: Add team absence count check before creating PENDING leave.

[ISSUE-012] BACKEND: WFH (Work From Home) Approval missing
CATEGORY: Backend Features
SEVERITY: MEDIUM
DETAIL:
  No WFH request model/API. Sirf Leave approval hai.
  Fix: Add WFH request schema, API, and frontend UI.

[ISSUE-013] BACKEND: Email Service integrate nahi
CATEGORY: Backend / External Integration
SEVERITY: CRITICAL
DETAIL:
  No nodemailer/Resend/SendGrid.
  Employee invite token generate hota hai but email nahi jaata.
  Admin manually token share karna padta hai — not scalable.
  Fix: npm install nodemailer (or @sendgrid/mail) + email templates.

[ISSUE-014] BACKEND: FCM Push Notifications nahi hain
CATEGORY: Backend / External Integration
SEVERITY: MEDIUM
DETAIL:
  No firebase-admin. Sirf WebSocket in-app notifications.
  No FCM device token field in User model.
  Fix: Add pushToken to User model + firebase-admin integration.

[ISSUE-015] BACKEND: PDF Export implement nahi (CSV partially done)
CATEGORY: Backend Features
SEVERITY: MEDIUM
DETAIL:
  CSV export endpoints exist (/reports/attendance/export etc.) — WORKING.
  PDF export nahi: pdfkit/puppeteer install nahi.
  Frontend ReportsView.tsx export button bhi wire nahi hua.
  Fix: Install pdfkit, wire frontend download buttons to export APIs.

[ISSUE-018] BACKEND: Duplicate Route Registration (Security Issue)
CATEGORY: Backend Structure / Security
SEVERITY: MEDIUM
DETAIL:
  routes/index.ts mein same controller for:
    apiRouter.use('/admin/employees', employeeRoutes)
    apiRouter.use('/employees', employeeRoutes)
  Admin endpoint pe admin-only middleware nahi. Same controller dono ke liye.
  Fix: Add requireRoles('ADMIN', 'HR_ADMIN') middleware on /admin/employees.

[ISSUE-019] BACKEND: No Validation Middleware (Zod/Joi)
CATEGORY: Backend Quality
SEVERITY: MEDIUM
DETAIL:
  No Zod/Joi. Controllers mein basic null checks only.
  Invalid data types, negative numbers, malformed dates — DB mein ja sakte hain.
  Fix: npm install zod + add schema validation on POST/PATCH routes.

[ISSUE-020] BACKEND: Error Handling inconsistent across modules
CATEGORY: Backend Quality
SEVERITY: LOW
DETAIL:
  errorHandler registered in server.ts (global).
  But modules inconsistently use it — some catch & respond directly.
  Fix: Standardize by throwing AppError instances to let global handler respond.

[ISSUE-021] FRONTEND STRUCTURE: Business-Flow folder structure follow nahi
CATEGORY: Frontend Structure
SEVERITY: LOW
DETAIL:
  views/ abhi bhi flat (22 files). layouts/ empty. No React Router DOM.
  pages/ folders exist but partially populated.
  Low priority — functionality not affected.

[ISSUE-022] FRONTEND: React Router implement nahi
CATEGORY: Frontend Architecture
SEVERITY: CRITICAL
DETAIL:
  Tab-based App.tsx routing. URL pe koi route nahi.
  Browser back/forward button kaam nahi karta.
  Deep linking nahi — kisi ko specific page ka link nahi bhej sakte.
  Fix: npm install react-router-dom + replace tab logic with URL routes.

[ISSUE-023] FRONTEND: No State Management Library
CATEGORY: Frontend Architecture
SEVERITY: LOW
DETAIL:
  Context API only. Acceptable for current scale.
  Fix later: Zustand (lightweight) ya Redux Toolkit.

[ISSUE-024] FRONTEND: No React Query / TanStack Query
CATEGORY: Frontend Architecture
SEVERITY: LOW
DETAIL:
  Direct axios in useEffect. No caching or auto-refetch.
  Fix later: npm install @tanstack/react-query.

[ISSUE-025] FRONTEND: Admin pages — client-side role check only
CATEGORY: Frontend Security
SEVERITY: MEDIUM
DETAIL:
  App.tsx mein role check client-side hi hai.
  localStorage mein role modify karke koi admin panel dekh sakta hai (UI level).
  Backend RBAC partially applied — some admin routes unprotected.
  Fix: Add requireRoles middleware consistently on all sensitive routes.

[ISSUE-031] PROJECT: No automation/testing setup
CATEGORY: Project Structure / Quality
SEVERITY: LOW
DETAIL:
  No Jest unit tests, no Playwright/Cypress E2E.
  Fix: Setup Vitest (frontend) + Supertest (backend API tests).

[ISSUE-032] PROJECT: Infrastructure/DevOps files missing
CATEGORY: Project Structure
SEVERITY: LOW
DETAIL:
  No docker-compose.yml, no Dockerfile.
  Fix: Create Dockerfile for server + docker-compose for full stack.

[ISSUE-033] PROJECT: API Documentation missing
CATEGORY: Project Structure
SEVERITY: LOW
DETAIL:
  No Swagger/OpenAPI spec.
  Fix: npm install swagger-ui-express + tsoa OR manual OpenAPI YAML.

[ISSUE-035] BUSINESS LOGIC: 72-Hour Invite Token Email nahi jaata
CATEGORY: Business Logic / External Integration
SEVERITY: CRITICAL
DETAIL:
  Token generate hota hai DB mein, email nahi jaata.
  Admin manually token copy karke employee ko dena padta hai.
  Depends on ISSUE-013 (Email service) being fixed first.

[ISSUE-036] BUSINESS LOGIC: Leave Balance Auto-Replenishment
CATEGORY: Business Logic
SEVERITY: LOW
DETAIL:
  jobs/scheduler.ts mein leave accrual cron aur manual trigger endpoints exist.
  Full year-end automation (carry-forward calculation + new-year reset) verify nahi hua.
  Needs end-to-end testing with actual date rollover.

[ISSUE-037] BUSINESS LOGIC: Mobile Push (FCM) Integration missing
CATEGORY: Business Logic / External Integration
SEVERITY: LOW
DETAIL:
  No firebase-admin. No FCM token field in User model.
  Depends on ISSUE-014 being fixed first.

[ISSUE-038] DOCUMENTATION: Timesheet feature Business-Flow.md mein mention nahi
CATEGORY: Documentation
SEVERITY: LOW
DETAIL:
  Timesheet feature fully implemented but Business-Flow.md not updated.
  Business-Flow.md mein add karna hoga: Employee timesheet log, Manager log, Admin monitor.

[ISSUE-039] NOTE: GET /attendance root => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  GET /attendance => 404 — this is expected (no root list needed).
  AttendanceView.tsx uses /attendance/my-timesheet (WORKS) and /attendance/today (WORKS).
  No fix required for root. If a paginated history view is added later, add endpoint then.

[ISSUE-041] NOTE: GET /timesheets root => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  Root /timesheets => 404. TimesheetsView uses /timesheets/my and /timesheets/all — both WORK.
  No fix required.

[ISSUE-042] NOTE: GET /reports root => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  Root /reports => 404. ReportsView uses specific sub-endpoints — all WORK.
  No fix required.

[ISSUE-043] NOTE: GET /payroll root => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  Root /payroll => 404. PayrollView uses /payroll/my-payslips, /payroll/structures etc. — WORK.
  No fix required.

[ISSUE-044] BACKEND: lifecycle/checklist endpoint missing
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  GET /lifecycle/checklist, /lifecycle/checklists => 404.
  LifecycleView uses /lifecycle/onboarding and /lifecycle/offboarding — both WORK.
  If checklist-specific endpoints needed, add them to lifecycleController.

[ISSUE-045] NOTE: GET /recruitment root => 404, /applications missing
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  Root /recruitment => 404. RecruitmentView uses /recruitment/jobs + /recruitment/pipeline — WORK.
  /recruitment/applications endpoint missing — not used by current frontend.
  Add when applications management UI is built.

[ISSUE-046] NOTE: GET /training root => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  Root /training => 404. TrainingView uses /training/programs — WORKS.
  No fix required for current UI.

[ISSUE-051] NOTE: /master/departments, /master/leave-types, /master/shifts => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  These sub-routes don't exist. All data available via /master/dropdowns (WORKS).
  No frontend component calls these directly — no fix required.

[ISSUE-055] NOTE: Login page pre-filled credentials visible
CATEGORY: Security/UX
SEVERITY: LOW
DETAIL:
  LoginView.tsx has demo credentials pre-filled (fine for dev/demo).
  Must remove before production deployment.
  Low priority — dev environment only concern.

[ISSUE-056] NOTE: /policies/grace-period => 404
CATEGORY: Backend API
SEVERITY: LOW
DETAIL:
  GET /policies/grace-period => 404. PolicyManagementView.tsx needs to be checked
  to see if it calls this endpoint. If not, no fix needed.
  Add endpoint if PolicyManagementView requires it.

[ISSUE-058] NOTE: CompanyDirectoryView — verify API
CATEGORY: Frontend
SEVERITY: LOW
DETAIL:
  CompanyDirectoryView.tsx exists. No /directory backend endpoint.
  Verify it uses /employees or /master/employees (now fixed) for its data.

================================================================================
PART 3: RESOLVED / CLOSED ISSUES
================================================================================

The following issues have been FIXED and REMOVED from active tracking:

ISSUE-007  [RESOLVED] Central routes/index.ts exists and works.
ISSUE-008  [RESOLVED] All major modules now implemented (22 modules).
ISSUE-009  [RESOLVED-PARTIAL] /policies/geofence endpoint exists. Clock-in accepts lat/lng.
ISSUE-010  [RESOLVED-PARTIAL] Clock-in API accepts coordinates in body.
ISSUE-016  [RESOLVED] Policy Management API — /policies/shifts and /policies/geofence work.
ISSUE-017  [RESOLVED] Roles API — /api/v1/roles works.
ISSUE-026  [RESOLVED] EmployeeProfileView.tsx exists with 'My Profile' sidebar tab.
ISSUE-027  [RESOLVED-PARTIAL] ManagerApprovalCenter.tsx exists — Leave + Regularization work.
ISSUE-028  [RESOLVED] TaskKanbanView.tsx exists in pages/manager/.
ISSUE-029  [RESOLVED] AuditLogsView.tsx exists. /audit-logs GET works.
ISSUE-030  [RESOLVED] PolicyManagementView.tsx exists. /policies/shifts GET works.
ISSUE-034  [CLOSED] Folder naming is a documentation inconsistency — no impact on functionality.
ISSUE-036  [RESOLVED-PARTIAL] Leave accrual cron + manual trigger endpoints exist.
ISSUE-038  [RESOLVED-PARTIAL] Timesheet feature implemented. Business-Flow.md update pending (low priority).
ISSUE-040  [CLOSED] Not a bug. TasksView.tsx correctly uses /tasks/me and /tasks/team.
ISSUE-047  [FIXED 22-Sep] Added /master/employees endpoint to masterDataController.ts.
           DocumentsView.tsx and AssetsView.tsx now work.
ISSUE-048  [FIXED 22-Sep] Added /regularize/pending alias to attendanceController.ts.
           AttendanceView.tsx regularization list now works.
ISSUE-049  [CLOSED] Not a bug. OverviewDashboard.tsx uses /dashboard (no /stats).
ISSUE-050  [CLOSED] Frontend correctly uses /leaves/balances (plural). No fix needed.
ISSUE-052  [FIXED 22-Sep] CORS restricted to localhost:5173, localhost:3000, and FRONTEND_URL env.
           No longer open to all origins.
ISSUE-053  [CLOSED] Auto-refresh already implemented in api.ts response interceptor.
           Reads refreshToken, calls /auth/refresh, retries original request.
ISSUE-054  [FIXED 22-Sep] Sidebar icons updated: Timesheets=FileCheck2, Recruitment=Target,
           Timesheet Compliance=BarChart3. No more duplicate Briefcase icons.
ISSUE-057  [CLOSED] Webmail feature is working (positive note, not a bug).

================================================================================
PART 4: WHAT IS WORKING (Verified 22-Sep-2026)
================================================================================

AUTH & SESSION:
  PASS  Login (JWT) — all 3 demo roles work
  PASS  JWT auto-refresh (via api.ts interceptor)
  PASS  Logout (refresh token revoked)
  PASS  Account activation via invite link (/set-password?token=...)
  PASS  RBAC middleware (authenticate, requireRoles, requirePermission)

NAVIGATION & UI:
  PASS  Role-based sidebar (tabs differ by role — ADMIN, MANAGER, EMPLOYEE)
  PASS  Theme toggle (Dark / Light)
  PASS  Mobile sidebar toggle
  PASS  Distinct sidebar icons (no duplicates — Fixed 22-Sep)
  PASS  Header: user name, role badge, clock, notification bell, logout

ATTENDANCE:
  PASS  Clock In  — POST /attendance/clock-in
  PASS  Clock Out — POST /attendance/clock-out
  PASS  Break Start/End — POST /attendance/break
  PASS  Today's status — GET /attendance/today
  PASS  Monthly timesheet — GET /attendance/my-timesheet
  PASS  Team Roster — GET /attendance/team-roster
  PASS  Regularization Request — POST /attendance/regularize
  PASS  Regularization List — GET /attendance/regularize/pending (Fixed 22-Sep)
  PASS  Regularize Approve/Reject — PATCH /attendance/regularize/:id/status
  PASS  Auto-clockout scheduler daemon running

LEAVES:
  PASS  Leave balance — GET /leaves/balances
  PASS  Apply Leave — POST /leaves/apply
  PASS  Leave requests list — GET /leaves/requests
  PASS  Leave history — GET /leaves/history
  PASS  Leave approve/reject — PATCH /leaves/:id/approve|reject
  PASS  Leave delegation — GET/POST /leaves/delegations
  PASS  Leave accrual cron (auto runs)
  PASS  Manual trigger — POST /leaves/admin/trigger-accrual

TASKS:
  PASS  Create Task — POST /tasks
  PASS  My tasks — GET /tasks/me
  PASS  Team tasks — GET /tasks/team
  PASS  Status update — PATCH /tasks/:id/status
  PASS  Delete task — DELETE /tasks/:id

TIMESHEETS:
  PASS  My timesheets — GET /timesheets/my
  PASS  All timesheets (Admin) — GET /timesheets/all
  PASS  Submit timesheet — POST /timesheets

BROADCASTS & NOTIFICATIONS:
  PASS  List broadcasts — GET /broadcasts
  PASS  Create broadcast — POST /broadcasts
  PASS  Notifications list — GET /notifications
  PASS  Mark all read — PATCH /notifications/mark-all-read
  PASS  WebSocket real-time events (Socket.io)

EMPLOYEES & ADMIN:
  PASS  Employee list — GET /employees (20 employees)
  PASS  Employee detail — GET /employees/:id
  PASS  Add employee — POST /admin/employees
  PASS  Employee status — PATCH /admin/employees/:id/status

MASTER DATA:
  PASS  Dropdowns — GET /master/dropdowns
  PASS  Employees list (for selectors) — GET /master/employees (Fixed 22-Sep)
  PASS  Holidays — GET /master/holidays

POLICIES & GOVERNANCE:
  PASS  Shifts — GET /policies/shifts
  PASS  Geofence rules — GET /policies/geofence
  PASS  Audit logs — GET /audit-logs
  PASS  Roles list — GET /roles

REPORTS:
  PASS  Attendance export — GET /reports/attendance/export
  PASS  Custom report modules — GET /reports/custom/modules
  PASS  Custom report preview — POST /reports/custom/preview

PAYROLL:
  PASS  My payslips — GET /payroll/my-payslips
  PASS  Salary structures — GET /payroll/structures
  PASS  Payroll runs — GET /payroll/runs

LIFECYCLE & RECRUITMENT:
  PASS  Onboarding list — GET /lifecycle/onboarding
  PASS  Offboarding list — GET /lifecycle/offboarding
  PASS  Job openings — GET /recruitment/jobs
  PASS  Hiring pipeline — GET /recruitment/pipeline

TRAINING & DOCS:
  PASS  Training programs — GET /training/programs
  PASS  Certifications — GET /training/certifications
  PASS  Documents — GET /documents
  PASS  Assets — GET /assets
  PASS  Assets by employee — now works (uses /master/employees Fixed 22-Sep)
  PASS  Documents by employee — now works (uses /master/employees Fixed 22-Sep)

WEBMAIL:
  PASS  Inbox — GET /mail/inbox
  PASS  Compose, reply, folders — working

SERVER HEALTH:
  PASS  Health check — GET /health
  PASS  Server starts clean (no errors in tsx watch)
  PASS  Cron scheduler initialized (leave accrual, auto-absent)

SECURITY (Post Fix 22-Sep):
  PASS  CORS restricted to localhost:5173, localhost:3000, FRONTEND_URL env
  PASS  JWT auto-refresh working (15min access + 7-day refresh)
  PASS  Audit logging middleware active

================================================================================
PART 5: PRIORITY ACTION ITEMS
================================================================================

P1 — Fix soon (user-visible impact):
  ISSUE-013  Email service (nodemailer/Resend) — invite flow broken without it
  ISSUE-022  React Router DOM — URL navigation, browser back/forward
  ISSUE-018  Admin route middleware separation (security)
  ISSUE-003  Rate limiting on /auth/login (brute force prevention)

P2 — Fix before production:
  ISSUE-005  File upload (multer + S3) — avatars, leave docs, task attachments
  ISSUE-019  Zod validation on all POST/PATCH endpoints
  ISSUE-025  Backend RBAC consistently on all sensitive routes
  ISSUE-055  Remove pre-filled demo credentials from LoginView.tsx

P3 — Plan for scale:
  ISSUE-001  PostgreSQL migration (SQLite -> PostgreSQL via Prisma)
  ISSUE-002  Redis (caching + session store)
  ISSUE-004  BullMQ (background job queue)

P4 — Nice to have:
  ISSUE-031  Testing setup (Vitest + Supertest + Playwright)
  ISSUE-032  Docker + docker-compose
  ISSUE-033  Swagger/OpenAPI documentation
  ISSUE-006  Module refactor (service/repository layers)
  ISSUE-022  React Router DOM

================================================================================
FIXES APPLIED — PATCH #1 (22-Sep-2026)
================================================================================

Fix 1: ISSUE-047 — server/src/modules/master/masterDataController.ts
  Added GET /master/employees endpoint.
  Returns slim employee list (id, name, code, designation, dept, avatar).
  DocumentsView.tsx and AssetsView.tsx now load employee list correctly.

Fix 2: ISSUE-048 — server/src/modules/attendance/attendanceController.ts
  Added '/regularize/pending' as alias alongside '/regularize/team'.
  AttendanceView.tsx line 202 api.get('/attendance/regularize/pending') now works.

Fix 3: ISSUE-052 — server/src/server.ts
  CORS changed from origin:'*' to allowedOrigins list.
  Allowed: localhost:5173, localhost:3000, FRONTEND_URL env variable.
  Requests without origin (curl, health checks) still allowed.

Fix 4: ISSUE-054 — client/src/components/Sidebar.tsx
  Added imports: FileCheck2, Target, BarChart3 from lucide-react.
  Timesheets: Briefcase -> FileCheck2
  Recruitment: Briefcase -> Target
  Timesheet Compliance: Briefcase -> BarChart3

Closed as NOT bugs (confirmed by code inspection):
  ISSUE-040: TasksView.tsx correctly uses /tasks/me and /tasks/team.
  ISSUE-049: OverviewDashboard.tsx correctly uses /dashboard (not /stats).
  ISSUE-050: Frontend correctly calls /leaves/balances (plural).
  ISSUE-053: api.ts already has JWT auto-refresh interceptor implemented.

================================================================================
END OF ISSUE TRACKER — Patch #1 Complete (22-Sep-2026)
================================================================================
